name: cubit
description: A standard Cubit with Equatable state
version: 0.1.0+1
environment:
  mason: ">=0.1.0-dev <0.1.0"
vars:
  name:
    type: string
    description: "The name of the cubit (e.g. auth, theme)"
    default: "my_cubit"
    prompt: "What is the cubit name?"
