name: bloc
description: A standalone BLoC with Equatable state
version: 0.1.0+1
environment:
  mason: ">=0.1.0-dev <0.1.0"
vars:
  name:
    type: string
    description: "The name of the bloc (e.g. auth, theme)"
    default: "my_bloc"
    prompt: "What is the bloc name?"
