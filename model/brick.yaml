name: model
description: A json_serializable Equatable model
version: 0.1.0+1
environment:
  mason: ">=0.1.0-dev <0.1.0"
vars:
  name:
    type: string
    description: "The name of the model (e.g. user, product)"
    default: "my_model"
    prompt: "What is the model name?"
